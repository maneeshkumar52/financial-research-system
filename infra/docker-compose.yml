version: "3.9"
services:
  financial-research:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    environment:
      - LOCAL_MODE=true
